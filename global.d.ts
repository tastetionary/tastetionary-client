// Declare the global interface for the window object
declare global {
  interface Window {
    kakao: any;
    naver: any;
  }
  const kakao: any;
}

// Export an empty object to satisfy the export {}
export {};
